#include <stdio.h>

/**
* main - Entry point
*
* Return: Always 0 (Success)
*/

int main(void)

{

int i;
char ch;

for (i = 0; i <= 9; i++)
{
putchar(i + '0');
}

for (ch = 'a'; ch <= 'f'; ch++)
{
putchar(ch);
}

return (0);

}
